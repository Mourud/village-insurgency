class Player{
  constructor(playerColor, moves, population ) {
    this.playerColor = playerColor;
    this.moves = moves;
  }

  makeVillager() {
    alert('It works')
    console.log('checks out')
  }
}